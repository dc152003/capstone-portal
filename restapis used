🔹 Authentication APIs (auth.js)

POST /signup → register HRs or candidates with validations, password hashing, JWT creation.

POST /login → login existing users, return JWT for authentication.

👉 Handles user identity, access control, and security.

🔹 Job Management APIs (jobs.js)

POST /jobs → create a new job posting (HR only).

GET /jobs → fetch all jobs.

GET /jobs/:id → fetch details of a single job.

PUT /jobs/:id → update an existing job (HR only).

DELETE /jobs/:id → delete a job (HR only).

👉 Enables HRs to manage recruitment postings.

🔹 Resume Handling APIs (resume.js)

POST /resumes/upload → upload a candidate resume (via multer), link it to a job + candidate.

GET /resumes/:jobId → get all resumes uploaded for a particular job.

👉 Manages candidate applications.

🔹 AI Resume Analysis APIs (ai.js)

POST /analyze/:jobId/:resumeId →

Extract text from resume PDF.

Send resume + job description to OpenAI.

Receive AI analysis (JSON with relevance score, skills, comments).

Save results in AIAnalysis collection.

GET /analysis/:jobId/:resumeId → fetch AI analysis of a candidate’s resume for a job.

👉 Automates shortlisting & insights.

🔹 Best Fit APIs (bestfit.js)

GET /bestfit/:jobId/:topN →

Rank all resumes for a job using AI analysis.

Return top N candidates with scores & comments.

GET /bestfit/report/:jobId/:topN → generate combined PDF report of top N candidates (ranked list).

GET /bestfit/report/:jobId/candidate/:resumeId → generate individual PDF report for one candidate.

👉 Provides AI-powered shortlist and reports for HRs.

🔹 Dashboard APIs (dashboard.js)

GET /dashboard/stats →

Count number of jobs, resumes, candidates, and unique applicants.

👉 Gives HRs a summary view of hiring activity.
